# Working Log (2026-04-29)

- 要件定義書を解析し、仕様不足を `docs/assumptions.md` に明文化。
- Clean Architecture 構成で `Domain/Application/Infrastructure/Api/Tests` を新規作成。
- 休暇申請、承認ルート、承認/差戻し(Reject)、取消、残数計算、ダッシュボード集計の主要ロジックを実装。
- JWT 認証、ロールベース認可、通知抽象、インメモリリポジトリを実装。
- 単体テスト（業務ルール・承認フロー）を追加。
- 設計書群、API 仕様書、テスト仕様書、README、セットアップスクリプトを作成。
- `dotnet build` / `dotnet test` で実行可能性を検証。
- Next.js フロントエンドを新規作成し、申請・承認・残数・ダッシュボード画面と API クライアントを実装。
- フロントエンドのテスト基盤（Vitest）を追加し、`npm run lint` / `npm run test` / `npm run build` を検証。
