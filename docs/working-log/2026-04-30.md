# Working Log (2026-04-30)

- フロントエンド(`http://localhost:3000`)からAPI(`http://localhost:5000`)への`PUT /api/routes`で発生するCORSエラーを再現確認。
- `src/ApproveFlow.Api/Program.cs` に開発向けCORSポリシーを追加。
- 許可オリジンを `http://localhost:3000` に限定し、`Authorization` / `Content-Type` ヘッダと `PUT` を含む必要メソッドを許可。
- `UseCors` を `UseAuthentication` / `UseAuthorization` より前に配置し、プリフライトが通るように修正。
- `dotnet build` / `dotnet test` で回帰がないことを確認。
